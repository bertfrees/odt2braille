# These properties can be edited

target.os=macosx_x86
oo.dir=/Applications/OpenOffice.org.app/Contents

# Optional

sdk.dir=/Applications/OpenOffice.org3.1_SDK

# These properties should be left unchanged

basis.dir=${oo.dir}${file.separator}basis-link
ure.dir=${basis.dir}${file.separator}ure-link
java.dir=${ure.dir}${file.separator}share${file.separator}java
classes.dir=${basis.dir}${file.separator}program${file.separator}classes
office.program.dir=${oo.dir}${file.separator}MacOS
oo.unotypes.rdb=-X${ure.dir}${file.separator}share${file.separator}misc${file.separator}types.rdb
oo.offapi.rdb=-X${basis.dir}${file.separator}program${file.separator}offapi.rdb
