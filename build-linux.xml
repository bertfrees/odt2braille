<?xml version="1.0" encoding="UTF-8"?>
<project name="Odt2BrailleAddOn" default="default" basedir=".">
    <description>Build the project on Linux without NetBeans.</description>
	<import file="build.xml"/>
		
	<target name="-init-linux">		
		<property file="nbproject/project.properties"/>
		<property file="nbproject/project-uno.properties"/>
		<property file="build-linux.properties"/>
	
		<available file="${dist.dir}${file.separator}${external.jar.dir}" property="dist.lib.dir.exists"/>		
	</target>
	
	<target name="-copy-lib" depends="-init-linux" unless="dist.lib.dir.exists">	
		<copy todir="${dist.dir}">
			<fileset dir="." includes="${external.jars}" />
		</copy>
	</target>
		
	<target name="-pre-init" depends="-init-linux,-copy-lib"/>	
	
</project>
