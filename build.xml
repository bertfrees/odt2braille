<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<project name="Odt2BrailleAddOn" default="default" basedir=".">
    <description>Builds, tests, and runs the project extension project.</description>
    <import file="nbproject/build-uno-impl.xml"/>
    <!--

    There exist several targets which are by default empty and which can be 
    used for execution of your tasks. These targets are usually executed 
    before and after some main targets. They are: 

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-jar:                  called before JAR building
      -post-jar:                 called after JAR building
      -pre-uno-package:          called before OXT creation
      -post-uno-package:         called after OXT creation
      -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported 
    nbproject/build-impl.xml file. 


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are: 

      -init-macrodef-javac:     defines macro for javac compilation
      -init-macrodef-junit:     defines macro for junit execution
      -init-macrodef-debug:     defines macro for class debugging
      -init-macrodef-java:      defines macro for class execution
      -do-jar-with-manifest:    JAR building (if you are using a manifest)
      -do-jar-without-manifest: JAR building (if you are not using a manifest)
      run:                      execution of project 
      -javadoc-build:           Javadoc generation
      test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

        <target name="run" depends="TypeBrowser2-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that the overridden target depends on the jar target and not only on 
    the compile target as the regular run target does. Again, for a list of available 
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file. 

    -->
	
        <!-- copy necessary .jar files to Odt2BrailleAddOn/dist/lib -->
        <target name="-post-jar">

                <echo message="post jar building" />

                <copy file="${reference.DaisyPipeline.jar}"        tofile="${dist.dir}/${external.jar.dir}/DaisyPipeline.jar"        overwrite="true" />
                <copy file="${reference.Odt2Braille.jar}"          tofile="${dist.dir}/${external.jar.dir}/Odt2Braille.jar"          overwrite="true" />
                <copy file="${reference.JODL.jar}"                 tofile="${dist.dir}/${external.jar.dir}/JODL.jar"                 overwrite="true" />
                <copy file="${file.reference.commons-cli-1.1.jar}" tofile="${dist.dir}/${external.jar.dir}/commons-cli-1.1.jar"      overwrite="true" />
                <copy file="${file.reference.commons-io-1.4.jar}"  tofile="${dist.dir}/${external.jar.dir}/commons-io-1.4.jar"       overwrite="true" />
                <copy file="${file.reference.jna.jar}"             tofile="${dist.dir}/${external.jar.dir}/jna.jar"                  overwrite="true" />
                <copy file="${file.reference.saxon9he.jar}"        tofile="${dist.dir}/${external.jar.dir}/saxon9he.jar"             overwrite="true" />
                <copy file="${file.reference.juh-2.1.0.jar}"       tofile="${dist.dir}/${external.jar.dir}/juh-2.1.0.jar"            overwrite="true" />
                <copy file="${file.reference.isorelax.jar}"        tofile="${dist.dir}/${external.jar.dir}/isorelax.jar"             overwrite="true" />
                <copy file="${file.reference.msv.jar}"             tofile="${dist.dir}/${external.jar.dir}/msv.jar"                  overwrite="true" />
                <copy file="${file.reference.org.daisy.util.jar}"  tofile="${dist.dir}/${external.jar.dir}/org.daisy.util.jar"       overwrite="true" />
                <copy file="${file.reference.relames.jar}"         tofile="${dist.dir}/${external.jar.dir}/relames.jar"              overwrite="true" />
                <copy file="${file.reference.relaxngDatatype.jar}" tofile="${dist.dir}/${external.jar.dir}/relaxngDatatype.jar"      overwrite="true" />
                <copy file="${file.reference.xalan.jar}"           tofile="${dist.dir}/${external.jar.dir}/xalan.jar"                overwrite="true" />
                <copy file="${file.reference.xsdlib.jar}"          tofile="${dist.dir}/${external.jar.dir}/xsdlib.jar"               overwrite="true" />
                <copy file="${file.reference.stax-api-1.0.1.jar}"  toFile="${dist.dir}/${external.jar.dir}/stax-api-1.0.1.jar"       overwrite="true" />

        </target>

        <!-- ask target.os as an input -->
        <target name="init-target-os">
                <input message="Choose extension target platform:"
                       validargs="${list.target.os}"
                       addproperty="target.os" />

                <condition property="windows_x86">
                        <equals arg1="${target.os}" arg2="windows_x86"/>
                </condition>
                <condition property="macosx_x86">
                        <equals arg1="${target.os}" arg2="macosx_x86"/>
                </condition>
        </target>
	
	<!-- copy windows binaries -->
	<target name="copy-binaries-win" if="windows_x86">	
		<copy file="${liblouis.bin.dir}${file.separator}xml2brl.exe" todir="${build.oxt.dir}${file.separator}${liblouis.bin.dir}" />
		<copy todir="${build.oxt.dir}${file.separator}${liblouis.bin.dir}">
			<fileset dir="${liblouis.bin.dir}">
				<filename name="*.dll"/>
			</fileset>
        </copy>
	</target>
	
	<!-- copy mac os binaries -->	
	<target name="copy-binaries-mac" if="macosx_x86">	
		<copy file="${liblouis.bin.dir}${file.separator}xml2brl" todir="${build.oxt.dir}${file.separator}${liblouis.bin.dir}" />
		<copy todir="${build.oxt.dir}${file.separator}${liblouis.bin.dir}">
			<fileset dir="${liblouis.bin.dir}">
				<filename name="*.dylib"/>
			</fileset>
        </copy>
	</target>
	
	<!-- rebuild extension with some fixes -->
    <target name="-post-uno-package" depends="init-target-os">

        <echo message="post uno packaging" />

        <!-- unzip extension in a clean dir -->
        <delete dir="${build.oxt.dir}" />
        <mkdir dir="${build.oxt.dir}" />
        <unzip src="${uno.package.name}" dest="${build.oxt.dir}" />

        <!-- add templates folder -->
        <copy todir="${build.oxt.dir}${file.separator}${templates.dir}">
            <fileset dir="${templates.dir}" />
        </copy>
		
		<!-- add liblouis folder -->
		
		<mkdir dir="${build.oxt.dir}${file.separator}${liblouis.dir}" />
        <copy todir="${build.oxt.dir}${file.separator}${liblouis.files.dir}">
            <fileset dir="${liblouis.files.dir}" />
        </copy>
		<antcall target="copy-binaries-win"/>
		<antcall target="copy-binaries-mac"/>
		
		<!-- add settings folder -->
        <copy todir="${build.oxt.dir}${file.separator}${settings.dir}">
            <fileset dir="${settings.dir}" />
        </copy>
		
		<!-- add licenses folder -->
        <copy todir="${build.oxt.dir}${file.separator}${licenses.dir}">
            <fileset dir="${licenses.dir}" />
        </copy>
		
		<!-- specify target in description.xml -->
        <delete file="${build.oxt.dir}${file.separator}description.xml" />
        <filter token="platform-oxt" value="${target.os}"/>
        <copy file="${src.dir}${file.separator}description.xml"
              tofile="${build.oxt.dir}${file.separator}description.xml"
              filtering="true" />

        <!-- zip the new extension addon -->
        <delete file="${uno.package.name}" />

        <zip destfile="${project.dir}${file.separator}${dist.dir}${file.separator}${project.name}.oxt"
             basedir="${build.oxt.dir}" />
             
    </target>
</project>
